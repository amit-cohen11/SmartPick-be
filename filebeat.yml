filebeat.inputs:
  - type: filestream
    enabled: true
    id: my-filestream-id
    paths:
      - /var/log/app/output-*.log
    prospector.scanner.fingerprint.length: 64

output.logstash:
  hosts: ["logstash:5044"]

setup.kibana:
  host: "kibana:5601"
